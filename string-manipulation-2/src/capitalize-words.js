/* exported capitalizeWords */

/* Pseudo Code

1. Function definition named capitalizeWord with one parameter, (word);
2. Use toUpperCase() method of the parameter object being called with no arguments, concatenated with, the toLowerCase() method being called with no arguments, of the slice() method being called with one argument number 1, of the parameter object.
3. 'If' statement with

*/

// function capitalizeWord(word) {
//   if (word.toLowerCase() === 'javascript') {
//     return word[0].toUpperCase() + word.slice(1, 4).toLowerCase() + word[4].toUpperCase() + word.slice(5).toLowerCase();
//   } else {
//     return word[0].toUpperCase() + word.slice(1).toLowerCase();
//   }
// }
