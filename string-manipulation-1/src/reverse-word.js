/* exported reverseWord */

/* Pseudo Code:
  Goal: Return a string containing the characters of the parameter in reverse order.

  1. Use a variable with an empty string as a placeholder.
  2. Use a 'for-loop' to loop over the string parameter.
  3. Set condition of 'for-loop' to be half of the length of the string parameter.
*/

// function reverseWord(word) {
//   var placeholder = '';
//   for (var i = 0; i < word.length / 2; i++) {
//     placeholder = word[i];
//     word[word.length - 1] += word[i];
//   }
//   return placeholder;
// }
