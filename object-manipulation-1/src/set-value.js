/* exported setValue */

/* Pseudo Code

1. Define a function named setValue, with three parameters (object, key, value).
2. Assign variable value to variable object at variable key;
*/

function setValue(object, key, value) {
  object[key] = value;
}
