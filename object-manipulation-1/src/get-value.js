/* exported getValue */

/* Pseudo Code

1. Define a function named getValue, with two parameters (object, key);
2. Return value of object at key property.
*/

function getValue(object, key) {
  return object[key];
}
